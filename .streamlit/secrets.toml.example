# Đổi tên file này thành secrets.toml để chạy local (KHÔNG commit file thật)
# Trên Streamlit Cloud: dán nội dung này vào phần Settings > Secrets
REPLICATE_API_TOKEN = "r8_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"
